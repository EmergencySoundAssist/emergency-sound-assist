# 하드웨어

## 구성
| 장비 | 역할 |
|------|------|
| **NVIDIA Jetson Orin Nano Super Dev Kit** | 메인 연산 (분류 모델·방향·접근 알고리즘 실행) |
| **microSD 128GB** | OS·코드·AI 모델 저장 |
| **ReSpeaker USB Mic Array (XVF-3000)** | 4-mic 어레이, 소리 수집 + 자체 DoA |

---

## ReSpeaker XVF-3000 채널 구성 (중요)

물리 마이크는 **4개**지만, USB로는 **6채널**이 나온다.

| 채널 | 내용 | 용도 |
|:----:|------|------|
| 0 | 처리된 오디오 (빔포밍/AEC, 1채널 깨끗) | **① 분류** |
| 1 | 마이크1 원본(raw) | ② 방향 / ③ 접근 |
| 2 | 마이크2 원본 | ② 방향 / ③ 접근 |
| 3 | 마이크3 원본 | ② 방향 / ③ 접근 |
| 4 | 마이크4 원본 | ② 방향 / ③ 접근 |
| 5 | 재생(playback) 참조 | 보통 미사용 |

- **기본 펌웨어가 6채널 제공** → 별도 플래시 불필요.
- 파이썬에서 `sounddevice`로 채널 6개로 열어 슬라이스:
  ```python
  data, _ = stream.read(n)   # (n, 6)
  processed = data[:, 0]     # 분류용
  raw4 = data[:, 1:5]        # 방향/접근용
  ```

### 자체 DoA 읽기 (별개 경로)
- 방향 값은 **오디오 스트림이 아니라 USB 제어 채널**로 읽음 (`pyusb` + `tuning.py`).
- `Tuning.direction` → 0~359° 정수. 녹음과 동시에 폴링 가능.

---

## 개발 단계 참고
- **노트북 단계(현재)**: 내장 마이크 1채널로 충분 (`audio/capture.py`).
- **Jetson 단계**: 위 6채널 구성으로 전환, ch0=분류 / ch1~4=방향·접근.
- 마이크 어레이 **장착 방향**(어느 마이크가 "전방")이 각도 기준 → 장착 기준 정해둘 것.
