# EmergencySoundAssist — finetune(파인튜닝 준비: 평가셋 v0 + baseline 측정) 의존성
# 설치: pip install -r finetune/requirements.txt
#
# ⚠️ Jetson 에는 설치하지 말 것 — 이 패키지는 데이터 준비·평가 전용(개발 머신).
# ⚠️ torch 를 끌어오지 않는다. datasets 는 Audio(decode=False) 경로만 사용하므로
#    torchcodec/torch 불필요 (finetune/corpora.py 참고).

datasets>=4.0          # HF 코퍼스 (zeroth/fleurs — parquet, 스크립트 불필요)
jiwer>=4.0             # CER/WER (4.0: reference/hypothesis 인자명)
edge-tts>=7.2          # 긴급문구 TTS 합성 (무료 Edge 엔드포인트, 네트워크 필요)
soxr                   # 리샘플링 (mp3 24k → 16k 등)
scipy                  # 확성기 시뮬 밴드패스 필터
soundfile              # 오디오 I/O (0.14+: mp3 읽기 지원 — ffmpeg 불필요)
numpy
