# emergency-sound-assist — DoA(방향추정) 의존성
# 설치: python3 -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt
#   ※ Jetson(JetPack/aarch64)은 conda 불가 → venv 사용. 상세: docs/doa/jetson.md
#   ※ 실행은 레포 루트에서 `python -m doa.multi_live` (모듈 실행)

# ── 공통 ─────────────────────────────────────────
numpy>=1.24

# ── 다중 음원 SRP/MUSIC (multi_live / diag / multi_source) ──
scipy>=1.10
sounddevice>=0.4          # 오디오 캡처  ※ Ubuntu/Jetson: sudo apt install libportaudio2
pyroomacoustics>=0.7      # SRP-PHAT / MUSIC 공간 스펙트럼

# ── ReSpeaker USB 제어 (자체 DoA 레지스터 + LED) ──
pyusb>=1.2               # ※ libusb 백엔드 필요: brew install libusb / apt install libusb-1.0-0

# ── 개발 ─────────────────────────────────────────
pytest>=7.0
