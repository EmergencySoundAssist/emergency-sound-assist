# EmergencySoundAssist 런타임 의존성
# 설치:  pip install -r requirements.txt

# ── 필수 (분류 ONNX 추론 + 도플러·멜·FFT) ─────────────────
numpy>=1.24
scipy>=1.10              # classifier 리샘플(16k→22.05k)
onnxruntime>=1.17        # cnn_attn_full ONNX 추론
                         #   Jetson은 onnxruntime-gpu 또는 TensorRT EP 사용

# ── 오디오 입력 ────────────────────────────────────────
# --mic (실시간 마이크): sounddevice  ※ Ubuntu/Jetson: sudo apt install libportaudio2
# --wav (파일 처리)    : soundfile
sounddevice>=0.4
soundfile>=0.12

# ── Jetson(보드) 주의 ──────────────────────────────────
#  - TensorRT는 JetPack에 기본 포함 → pip 설치 X (trtexec로 .engine 변환)
#  - onnxruntime-gpu / torch 는 NVIDIA 전용 aarch64 빌드 사용 (일반 pip 아님)
